# Copy the code beneath the 'Write the rest of the program here' text.
# Put the print command inside the definition to avoid an error.

def cups_to_grams(cups, density):
  # Convert cups to grams here.
  # The density is in grams-per-cup.
  print("no i don't think i will")
                         
# Write the rest of your program here
food = input('What food? ')
cups = input('How much in cups? ')
density = input('How many grams per cup? ')
grams = float(cups) * float(density)
print(float(cups), 'cups of', food, 'is', round(grams, 1), 'grams.')
